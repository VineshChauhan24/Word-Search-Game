<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.aar.app.wordsearch">

    <application
        android:name="com.aar.app.wordsearch.WordSearchApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".settings.SettingsActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".gameplay.GamePlayActivity">
        </activity>
        <activity android:name=".mainmenu.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".gameover.GameOverActivity">
        </activity>
    </application>

</manifest>